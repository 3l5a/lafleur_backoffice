{% extends 'base.html.twig' %}

{% block title %}Color
{% endblock %}

{% block body %}
	<h1>Color</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>N°</th>
				<td>{{ color.id }}</td>
			</tr>
			<tr>
				<th>NameColor</th>
				<td>{{ color.nameColor }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_color_index') }}">Retour à la liste</a>

	<a href="{{ path('app_color_edit', {'id': color.id}) }}">Éditer</a>

	{{ include('color/_delete_form.html.twig') }}
{% endblock %}
