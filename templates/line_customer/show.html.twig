{% extends 'base.html.twig' %}

{% block title %}LineCustomer
{% endblock %}

{% block body %}
	<h1>LineCustomer</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>QuantityLineCustomer</th>
				<td>{{ line_customer.quantityLineCustomer }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_line_customer_index') }}">Retour à la liste</a>

	<a href="{{ path('app_line_customer_edit', {'prize': line_customer.prize}) }}">Éditer</a>

	{{ include('line_customer/_delete_form.html.twig') }}
{% endblock %}
