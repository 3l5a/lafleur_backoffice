{% extends 'base.html.twig' %}

{% block title %}Measurement
{% endblock %}

{% block body %}
	<h1>Measurement</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>N°</th>
				<td>{{ measurement.id }}</td>
			</tr>
			<tr>
				<th>UnitMeasurement</th>
				<td>{{ measurement.unitMeasurement }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_measurement_index') }}">Retour à la liste</a>

	<a href="{{ path('app_measurement_edit', {'id': measurement.id}) }}">Éditer</a>

	{{ include('measurement/_delete_form.html.twig') }}
{% endblock %}
