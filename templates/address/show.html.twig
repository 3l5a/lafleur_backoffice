{% extends 'base.html.twig' %}

{% block title %}Address
{% endblock %}

{% block body %}
	<h1>Address</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>N°</th>
				<td>{{ address.id }}</td>
			</tr>
			<tr>
				<th>Line1Adress</th>
				<td>{{ address.line1Adress }}</td>
			</tr>
			<tr>
				<th>Line2Adress</th>
				<td>{{ address.line2Adress }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_address_index') }}">Retour à la liste</a>

	<a href="{{ path('app_address_edit', {'id': address.id}) }}">Éditer</a>

	{{ include('address/_delete_form.html.twig') }}
{% endblock %}
