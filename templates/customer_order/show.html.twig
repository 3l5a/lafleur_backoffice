{% extends 'base.html.twig' %}

{% block title %}CustomerOrder
{% endblock %}

{% block body %}
	<h1>CustomerOrder</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>N°</th>
				<td>{{ customer_order.id }}</td>
			</tr>
			<tr>
				<th>DateCustomerOrder</th>
				<td>{{ customer_order.dateCustomerOrder ? customer_order.dateCustomerOrder|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>DeliveryDateCustomerOrder</th>
				<td>{{ customer_order.deliveryDateCustomerOrder ? customer_order.deliveryDateCustomerOrder|date('Y-m-d') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_customer_order_index') }}">Retour à la liste</a>

	<a href="{{ path('app_customer_order_edit', {'id': customer_order.id}) }}">Éditer</a>

	{{ include('customer_order/_delete_form.html.twig') }}
{% endblock %}
