{% extends 'base.html.twig' %}

{% block title %}Prize
{% endblock %}

{% block body %}
	<h1>Prize</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>N°</th>
				<td>{{ prize.id }}</td>
			</tr>
			<tr>
				<th>NamePrize</th>
				<td>{{ prize.namePrize }}</td>
			</tr>
			<tr>
				<th>QuantityPrize</th>
				<td>{{ prize.quantityPrize }}</td>
			</tr>
			<tr>
				<th>DescriptionPrize</th>
				<td>{{ prize.descriptionPrize }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_prize_index') }}">Retour à la liste</a>

	<a href="{{ path('app_prize_edit', {'id': prize.id}) }}">Éditer</a>

	{{ include('prize/_delete_form.html.twig') }}
{% endblock %}
