{% extends 'base.html.twig' %}

{% block title %}Customer
{% endblock %}

{% block body %}
	<h1>Customer</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>N°</th>
				<td>{{ customer.id }}</td>
			</tr>
			<tr>
				<th>FirstNameCustomer</th>
				<td>{{ customer.firstNameCustomer }}</td>
			</tr>
			<tr>
				<th>LastNameCustomer</th>
				<td>{{ customer.lastNameCustomer }}</td>
			</tr>
			<tr>
				<th>EmailCustomer</th>
				<td>{{ customer.emailCustomer }}</td>
			</tr>
			<tr>
				<th>PasswordCustomer</th>
				<td>{{ customer.passwordCustomer }}</td>
			</tr>
			<tr>
				<th>Role</th>
				<td>{{ customer.role }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_customer_index') }}">Retour à la liste</a>

	<a href="{{ path('app_customer_edit', {'id': customer.id}) }}">Éditer</a>

	{{ include('customer/_delete_form.html.twig') }}
{% endblock %}
